package graph

import (
	"testing"
	"fmt"
)

func TestGraphCreation(t *testing.T) {

	sourceNode := Node{}
	destNode := Node{}
	
	fmt.Printf("src = %v, dest = %v", sourceNode, destNode)
	
/*
	if x := Sqrt(in); x != out {
		t.Errorf("Sqrt(%v) = %v, want %v", in, x, out)
        }
        */
}